<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

## Cursor Cloud specific instructions

- Dev server: `npm run dev` binds `--hostname :: --port 3000`. Restart it after switching branches.
- `npm test` uses Node's test runner (`node --experimental-strip-types --test`). Modules under test cannot import `@/` aliases; keep unit tests pointed at self-contained files (see `package.json` `test` script).
- `npm run typecheck` needs `.next` types from a prior `npm run build` (`LayoutProps`). If typecheck fails on generated Next types, build first.
- Attribution Models / Journeys / Attribution Overview use `getCanonicalAttributedOrders()` (identity → session → one eligible touch). Default window is **7 days** and is **pending revalidation** after migration 005. Re-run `bigquery/validation/11_conversion_lag_distribution.sql` before treating 7d as production-verified. 90-day is hidden until raw_events_full retention covers it.
- Canonical Shopify/BigQuery attribution failures must surface as error/unavailable (`EmptyPanel` / ConnectionStatus error), never as a believable $0 attributed result. `getCanonicalAttributedOrders()` throws on Shopify error and BigQuery query failure; do not `.catch(() => [])` on decision-making surfaces.
- Our Paid ROAS includes a paid channel's attributed revenue only when that channel has a numeric spend source. GoodsNova: Meta is trusted; Google/TikTok/Microsoft must not enter the Paid ROAS numerator while their spend is unavailable.
- Winner-take-all models (`first_touch`, `last_touch`, `last_non_direct`) break same-timestamp ties with `touchpoint_id` (TypeScript `timestamp, id` order; SQL `ROW_NUMBER`). Only one touch receives 100% credit.
- `getAttributionMetrics()` is legacy event-grain diagnostics (health / data-quality / first-touch Stape comparison). Do not use it for Models, Journeys, Overview comparison, or the order debugger.
- First-touch (cart gn_*) is `/attribution`. Multi-touch OUR attribution is `/attribution/overview`. Warehouse QA is `/warehouse`.
- MER = Shopify revenue ÷ ad spend. Marketing cost ratio = spend ÷ revenue. Unknown ≠ Direct. Assists = middle touches, not Linear.
- Do not invent Meta campaign-level nCAC. Campaign OUR revenue joins only on campaign id/name match. Purchase-event grain 2026-08-01–19: 39/72 purchases had fbclid; **0/72 had utm_campaign on the purchase URL** (checkout pages). Re-run `bigquery/validation/05_meta_campaign_mapping_coverage.sql` for touch-grain campaign rates. UI mapping chip stays VALIDATION REQUIRED until that touch-grain rate is recorded. Only 1 Meta campaign fact row in that range — do not invent campaign nCAC from that.
- Customer selected-history LTV uses Shopify orders loaded for the header range (up to 10k) and is labeled incomplete. It is not true first-purchase LTV.
- Web GTM stitch-fill for **GTM-MVWKFXH2 is already published** (Julio). Do not ask to re-import `gtm/import/GTM-MVWKFXH2_stitch-fill.json`. Shop Pay still does not run the stitch HTML. If `gn_uid` / click IDs are still empty in BigQuery, that is the **server** writer (`GTM-NJ4QCWFK` / `bigquery/analytics/GTM_CHANGES.md`), not a missing web-container publish.
- Flyweel is **campaign-grain only**. Empty Flyweel adset/ad fact tables do **not** mean GoodsNova lacks first-party child IDs. `/meta` primary UX is a campaign performance grid (mapping is a column/filter, not matched/unmatched sections). `/meta/story` is screenshot fixture data only and must never enter production loaders; it is allowed in local/Preview and returns 404 when `VERCEL_ENV === "production"`. Ad-set names are not in the live URL contract — display `Ad Set {short id}` unless a future explicit name parameter exists. Do not call observed child IDs HIGH / Ads Manager / Flyweel-verified. Platform child spend/ROAS/CPA stay unavailable. Campaign-name fallback URL-decodes `+` / `%7C` for PARTIAL unique-name matching only. `prefetch={false}` on Meta hierarchy links. Never put `cookies()` / `getSelectedPeriod()` under `unstable_cache`.
- Meta **channel** attribution may exist without campaign mapping. Exact first-party **session** IDs (`gn_meta_campaign_id` / `gn_meta_adset_id` / `gn_meta_ad_id` on the landing URL or current-session cookies) are authoritative HIGH joins. Browser `gn_session_meta_v1` / `gn_meta_*` cookies use a **30-minute inactivity TTL** (not raw browser-tab lifetime). Shopify cart `gn_first_meta_*` is 365-day first-touch audit only — never the converting campaign. Typed BQ `meta_*` columns are CURRENT SESSION / CLICK identity, never first-touch cookies. Same-session canonical ID disagreements (`campaignIdConflict` / `adsetIdConflict` / `adIdConflict`) are `SESSION_ID_CONFLICT` and are not HIGH-mapped at child grains. Conflicting campaign/adset or adset/ad pairs vs Meta fact parents are `META_HIERARCHY_CONFLICT`. Both reasons are NONE mapping confidence; channel Meta credit is never dropped. Campaign-name fallback is legacy PARTIAL only. Never fuzzy-map Meta campaigns. Never infer ad/adset IDs from aggregate Meta facts. Unmapped Meta credit must remain visible. Observed first-party adset/ad IDs are shown even when Flyweel child facts are empty; they are not platform-verified. Do not modify canonical attribution math for Meta identity enrichment. Warehouse SQL extracts IDs from `page_location`; do not reference typed `raw_events_full.meta_*` columns until migration 006 is confirmed. Acquisition `ARRAY_AGG` must order by `event_timestamp, event_id`. Validation 17 joins `v_attribution_credit_v1.touchpoint_id` to the canonical SHA256 session touch — do not use the order's last Meta-ID session. Web GTM Meta-ID changes are **click-by-click in the live workspace** (`docs/GTM_MANUAL_CHANGES.md`). **DO NOT MERGE AN OLD EXPORT OVER A NEWER LIVE CONTAINER.** Ads Manager URL contract: `docs/META_ATTRIBUTION_SETUP.md` (**MANUAL META UI VERIFICATION REQUIRED** — do not claim `{{campaign.id}}` / `{{adset.id}}` / `{{ad.id}}` independently verified). Historical unmapped Meta touches stay unmapped.
- Click-id fill is **live as of 2026-08-15** on both `Data Client` and `GA4` copies of `page_view` in `raw_events_full`. Dual GA4 + Data Client rows are expected. **`fbc` / `fbp` are not columns** on `raw_events_full` — Meta is `fbclid` + URL `fbclid`. Selecting `fbc` yields `Unrecognized name: fbc`. **No Google Ads account for now**. Residual `gclid` may be leftover URLs or Shopify Shop campaigns. Keep mapping `gclid`. Do not build a Google Ads product. Paid traffic to diagnose is Meta/`fbclid`.
- Do not copy `.env.example` verbatim in this VM: a placeholder `GOOGLE_CLOUD_PROJECT` plus a missing service-account file surfaces as a red Stape error. Leave BigQuery project empty when credentials are not present.
- Flyweel Meta ingest is **campaign-grain only**. `query_metrics` ads dimensions do not include adset/ad IDs. `FLYWEEL_INGEST_LEVELS=all` cannot enable unverified child grains. Do not count campaign rows as adset/ad rows. Do not populate `meta_adset_insights_daily` / `meta_ad_insights_daily` from campaign facts. When Flyweel is active, dashboard loaders must skip child-grain BigQuery (`getAdsetFacts` / `getAdFacts` / `getAdCreativeMap` return empty collections). Health is **partial/healthy**, not a red Meta error, when campaign reporting works. Flyweel `campaign_id` is a Flyweel UUID, not Meta `{{campaign.id}}` — exact ID HIGH mapping is unavailable; unique name may stay PARTIAL. See `src/lib/ads/providers/flyweel-query.ts`.
- Dashboard navigation must use `prefetch={false}` on the sidebar, bottom nav, and large row tables. Do not let Next.js viewport-prefetch every dashboard route. Desktop sidebar may hover-intent prefetch one href after a short delay.
- Expensive Shopify / Stape / Meta / warehouse loaders use tagged `unstable_cache` (~45s) keyed by period primitives. Never call `cookies()` inside a cached function. Ordinary Summary → Sales → Products navigation keeps that 45s cache. Explicit Refresh (Meta / Shopify / GA4 / Stape) must hard-expire via `revalidateTag(tag, { expire: 0 })` in the Route Handler — never `"max"` (SWR). `/api/meta/refresh` and `/api/meta/sync` cannot call `updateTag` (Server Actions only). Cron may use SWR `"max"`. After a successful COGS save, `updateTag("cogs")` expires core dashboard only (not Shopify / Meta / Stape facts). Spend paste uses `updateTag("paste")`. Unconfigured Google Ads is **disconnected**, never a red error. After successful Meta ingest, invalidate `meta` + `dashboard-core` + `health` + `attribution` — not Shopify. After successful Shopify ingest, invalidate `shopify` + `dashboard-core` + `health` + `attribution` — not Meta facts.
- There is no local `DASHBOARD_PASSWORD` in this environment, so `src/proxy.ts` does not gate pages.
- `POST /api/meta/sync` is the **worker** (`maxDuration` 300). Manual UI uses `POST /api/meta/refresh`, which returns HTTP 202 immediately and continues the job with Next.js `after()`. Do not make the browser wait for Flyweel. Verify the deployed Production runtime accepts maxDuration=300. Vercel cookie locks in `durable-json` are **not** a distributed lock — do not treat them as one. A `sync_runs` row with `status=running` older than 300s is stale/timed-out, not actively running. Do not rewrite those historical rows. If both BigQuery `sync_runs` terminal writes fail, log `warehouse_finish_error` and surface it in health; do not swallow it.
- Dashboard GET pages are **read operations**. They must not synchronously ingest providers (`getInsights()`, Flyweel `query_metrics`, Shopify Admin sync-all, GA4 pulls). Provider work belongs in background routes: `/api/cron/meta`, `/api/cron/shopify`, `/api/cron/ga4`, `/api/cron/stape`, `/api/cron/daily`, and `/api/meta/refresh` → `after()`. `source=all` / `/api/cron/sync` is admin-only sequential reconciliation, not production freshness.
- Dashboard reads prepared / last-known-good data (BigQuery facts + Next.js Data Cache + in-memory stale fallback). A running Meta job must not blank Overview / Sales / Attribution / Meta. Compact header copy is "Updated Xm ago" or "Updating…". Engineering diagnostics stay on `/health` and `/data-quality`.
- Shopify financial truth remains Shopify `currentTotalPriceSet` / net-after-refund. Canonical attribution remains `getCanonicalAttributedOrders()`. Do not create a second attribution definition. Prefer `analytics.fct_shopify_orders` when that table has rows for the header range (or when `shopify_ingest_coverage` marks the range empty-on-purpose). Admin API is fallback if the table is missing or the query fails. Shopify coverage checkpoint is `analytics.shopify_ingest_coverage` in BigQuery — never cookie `durable-json` (cron cookies do not reach dashboard readers). Unsupported provider grains are unavailable, never fabricated. Flyweel internal UUIDs are not native Meta IDs.
- Production freshness jobs are independent daily Vercel crons (Hobby rejected sub-daily `*/5` at deploy time). Meta 14:00, Shopify 15:00, GA4 16:00, Stape health 17:00, daily parallel recon 18:00 UTC. Do not resurrect a sequential "sync all" as the normal path. Do not put `*/5` in `vercel.json` until the connected Vercel project accepts it. See `docs/BACKGROUND_INGESTION.md`.
